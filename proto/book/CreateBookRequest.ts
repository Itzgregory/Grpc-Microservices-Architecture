export interface CreateBookRequest {
  'title'?: (string);
  'authorId'?: (string);
  'publishedYear'?: (number);
}

export interface CreateBookRequest__Output {
  'title': (string);
  'authorId': (string);
  'publishedYear': (number);
}
